if(k>str.length())
        return 0;
        str = str.substring(0,k);
        int count = 0;
        for(int i=0;i<n;i++){
            if(arr[i].indexOf(str) == 0)
            count++;
        }
        return count;
